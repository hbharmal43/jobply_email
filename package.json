{
  "name": "jobply-email",
  "version": "0.1.0",
  "private": true,
  "description": "Jobply lifecycle email subsystem — Part A (SES feedback ingestion) + Part B (outbound send pipeline)",
  "scripts": {
    "build": "tsc --noEmit",
    "deploy": "cdk deploy",
    "diff": "cdk diff",
    "synth": "cdk synth"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@aws-sdk/client-sesv2": "^3.700.0",
    "@aws-sdk/client-sqs": "^3.700.0",
    "@supabase/supabase-js": "^2.47.0",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "aws-cdk": "^2.170.0",
    "esbuild": "^0.24.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  }
}
